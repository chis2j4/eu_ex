name: 'EUserv Auto Extend'

on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    - cron: '50 0 * * *'

jobs:
  auto_renew:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout codes'
        uses: actions/checkout@v2
